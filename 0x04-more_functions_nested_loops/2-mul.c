#include "main.h"

/**
 * mul - Checks for a digit (0 through 9)
 * @a: a - Variable
 * @b: b - Variable
 *
 * Return: Always 0 (Success)
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
